interface:
  display_name: "Chatium"
  short_description: "Work safely in Chatium projects synced by the VS Code extension."
  default_prompt: "Use the Chatium skill to initialize auth, pull external changes, create a baseline, and publish local changes for this synced project."
