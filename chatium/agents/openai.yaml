interface:
  display_name: "Chatium"
  short_description: "Work safely in Chatium projects synced by the VS Code extension."
  default_prompt: "Use the Chatium skill flow strictly: for a new task run begin exactly once and never continue; when resuming the same task run continue and never begin."
