interface:
  display_name: "Chatium"
  short_description: "Work safely in Chatium projects synced by the VS Code extension."
  default_prompt: "Use the Chatium skill to run begin before reading project source, planning, or editing; begin refreshes server code and generated typings before creating the baseline."
