interface:
  display_name: "Chatium"
  short_description: "Work safely in Chatium projects synced by the VS Code extension."
  default_prompt: "Use the Chatium skill flow strictly: always run begin before source inspection; begin preserves current local changes, refreshes the server baseline, and reapplies them; run finish after edits to upload while leaving local changes uncommitted for review."
